import 'package:flutter/material.dart';

class CompeletSchedule extends StatelessWidget {
  const CompeletSchedule({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}